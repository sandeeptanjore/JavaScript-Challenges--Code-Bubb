/**
 * Exercise 05 - Simple calculator
 * Write a console statement that displays two numbers and then displays
 * the result of adding them together
 */

let result, firstNumber, secondNumber;

firstNumber = 10;
secondNumber = 20;
result = firstNumber + secondNumber;

console.log(`The sum of ${firstNumber} and ${secondNumber} is: ${result}`);
