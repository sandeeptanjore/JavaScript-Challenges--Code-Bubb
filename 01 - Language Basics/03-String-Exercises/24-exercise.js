/**
 * Exercise 24 - Remove the spaces
 * Remove the leading and trailing space from the following string:
 * const str = ' Lots of leading and trailing space ';
 * You can log the result out to the console to check.
 */

const str = ' Lots of leading and trailing space ';
const cleanString = str.trim()
console.log(cleanString);