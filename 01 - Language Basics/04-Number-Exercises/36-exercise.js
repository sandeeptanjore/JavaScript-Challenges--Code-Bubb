/**
 * Exercise 36 - Round 'em down
 * Given the following number:
 * const num = 3.49;
 * Use JavaScript to round the number to 3 and display the result in the console
 */

const num = 3.49;
let result = Math.round(num);
console.log(`The result of rounding is ${result}`);