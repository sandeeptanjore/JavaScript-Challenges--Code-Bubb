/**
 * Exercise 66 - You need A or B or C
 * Given the two variables
 * let A = false;
 *  let B = true;
 *  let C = false;
 * Use a JavaScript logical operator to create a boolean expression
 * which only results in true when both A or B are true
 */

let A = false;
let B = true;
let C = false;

console.log(A || B || C);
