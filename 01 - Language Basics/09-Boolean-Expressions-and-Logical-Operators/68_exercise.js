/**
 * Exercise 68 - You should be at least 21 years old
 * Given the variable:
 * let age =20;
 
 * Write a boolean expression (which will result in false) to determine whether the value stored in the 
 * age variable is at least 21. Check that expression is correct by changing the value stored in age to 
 * 21 and 22.
 */

let age = 20;

// if (age >= 21) {
//   console.log(`Your age is ${age}`);
// } else {
//   console.log(`The age is incorrect`);
// }

//Optimised code as per chatGPT:
console.log(age >= 21); // generates false
