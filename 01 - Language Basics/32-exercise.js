/**
 * Exercise 32 - Divide this by this
 * Given the following variables:
 * const g = 90;
 * const h = 15;
 * Use JavaScript to divide the value stored in g by h and display the result in the console.
 */

const g = 90;
const h = 15;
let result = g/h
console.log(`The result of the division is: ${result}`);