/**
 * Exercise 41 - 1 to 100 Create a random number with JavaScript in the range of 1 to 100.
 */

const result = Math.floor(Math.random() * 100) +1;
console.log(`Random number generated between 1 and 100 is: ${result}`);